node_modules

# Next.js build output
.next
out
dist

# TypeScript build info
*.tsbuildinfo

# Local env files
.env*

# Agent instructions (non-git-tracked)
AGENTS.md

# OS and editor files
.DS_Store
.cache

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Test coverage
coverage/
.nyc_output/

# ESLint cache
.eslintcache

# IDEs and editors
.vscode/
.idea/
*.sublime-workspace

# OS
Thumbs.db

# Yarn/PnP
.pnp.*
.yarn/*
.yarnrc.yml

# Node build cache / temporary
.cache/
*.local

# Agent working files (audit reports, ralph task state, etc.)
.audit/
.ralph/
.pi/

# Test/debug artifacts
.playwright-report/
test-results/
.playwright/
.research/

# Ignore posts/ and talks/
# Ignore posts/ and talks/

# Resume PDF snapshots — generated at build time from local LaTeX repo
# (mirrored by scripts/generate-pdf-snapshots.ts). Never committed.
public/resumes/
# nav-redesign Phase B (2026-07-12): src/data/resumes/*.json are
# build-regenerated artefacts from scripts/generate-json-resume.ts
# (LaTeX → JSON Resume). Tracking them creates noisy diffs and
# fights the pipeline. They are regenerated by `npm run
# generate:resumes` (called from `npm run build:resume-assets`).
src/data/resumes/*.json
