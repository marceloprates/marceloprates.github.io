node_modules

# Next.js build output
.next
out
dist

# TypeScript build info
*.tsbuildinfo

# Local env files
.env*

# Agent instructions (non-git-tracked)
AGENTS.md

# OS and editor files
.DS_Store
.cache

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Test coverage
coverage/
.nyc_output/

# ESLint cache
.eslintcache

# IDEs and editors
.vscode/
.idea/
*.sublime-workspace

# OS
Thumbs.db

# Yarn/PnP
.pnp.*
.yarn/*
.yarnrc.yml

# Node build cache / temporary
.cache/
*.local

# Agent working files (audit reports, ralph task state, etc.)
.audit/
.ralph/
.pi/

# Test/debug artifacts
.playwright-report/
test-results/
.playwright/
.research/

# Ignore posts/ and talks/
# Ignore posts/ and talks/

# Local drafts — kept on disk, untracked, hidden from the public site
# via the `draft: true` frontmatter flag (see src/lib/content.ts
# getAllPosts / getPostBySlug). 2026-07-12.
content/posts/2024-03-17-ia-no-significa-nada.md

# Resume JSONs and PDFs are committed artefacts (reverted from the
# 2026-07-12 nav-redesign Phase B "gitignore + regenerate at build"
# approach on 2026-07-18: the LaTeX repo is private, so CI cannot
# fetch it, so build-time regeneration ships empty CVs to production).
# Source of truth is still the local LaTeX repo — re-run
# `npm run generate:resume-assets` and commit the regenerated files
# when the LaTeX source changes. The committed-json test fails loudly
# if any variant's work[] silently empties.
# nav-redesign Phase F: public/search-index.json is the ⌘K palette
# data source, generated by `npm run build:search-index` from
# scripts/build-search-index.ts. Tracked nowhere; fresh clones run
# the script during `npm run build`.
public/search-index.json

# private-portfolio-candidates Phase B (2026-07-12):
# `npm run scan:portfolio` produces a per-repo candidate report
# (visibility + include flag + tier + summary + tags). Gitignored
# because it's regenerated on every scan; the source of truth is
# each repo's portfolio.md + portfolio-decisions.json.
portfolio-candidates.md
portfolio-manifests-to-seed/
portfolio-bodies/
